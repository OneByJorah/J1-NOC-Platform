# J1 NOC Windows Agent Dependencies
# Install with: pip install -r requirements.txt

# Core
psutil==5.9.8
httpx==0.27.0

# Windows-specific (only install on Windows)
pywin32==306; sys_platform == "win32"

# Build
pyinstaller==6.8.0; sys_platform == "win32"